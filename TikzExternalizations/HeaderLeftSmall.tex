\documentclass[a4paper, 10pt, twocolumn, twoside]{book}
\usepackage[dvipdfx, rgb]{xcolor}
  \definecolor{yellowtext}{HTML}{f9b00b}
  \definecolor{redtext}{HTML}{931004}
  \definecolor{whitetext}{gray}{0.5}
  
  \definecolor{normalpage}{gray}{0.95}
  \definecolor{storyblack}{gray}{0.1}
  \definecolor{boxred}{HTML}{721a16}
  \definecolor{headred}{HTML}{5e1519}

\usepackage{tikz}
\usetikzlibrary{fadings}
\usetikzlibrary{patterns}
\usetikzlibrary{external}
\usetikzlibrary{calc}
%\tikzexternalize[prefix=figures/]


\usepackage{geometry}
  \geometry{a4paper, 
  includeheadfoot,
  top=0cm, 
  headheight=23mm, 
  headsep=7mm, 
  footskip=23mm, 
  bottom=7mm,
  left=20mm, 
  right=15mm
  }  
  
\graphicspath{{./SR5_Fankit/}}
  

\pagestyle{empty}
\begin{document}
\begin{tikzpicture}[remember picture, overlay, shift={(current page.north west)}]
	  \fill[fill=storyblack] (0,0) rectangle (\paperwidth, -\headheight+3mm);  
	  \fill[headred, path fading=west] (0,0) rectangle (\paperwidth, -\headheight+3mm);
	  \fill[pattern=dots, pattern color=white, path fading=west, opacity=0.05] (0,0) rectangle (\paperwidth, -\headheight+3mm);

	  \pgfmathsetseed{534532}
	  
	  \edef\srtmp{0.5}
      \foreach \i in {0,2,...,208} {
      \pgfmathparse{\srtmp + (rand*(rand * 1 ))}
	  \edef\srtmp{\pgfmathresult}
      \pgfmathparse{(208-\i)/208*100}
      \edef\c{\pgfmathresult}
      
      \fill[normalpage] (\i mm, -\headheight+1mm) rectangle ++(2mm, 2mm); 
      \fill[storyblack!\c!headred, opacity={\srtmp - .1}] (\i mm, -\headheight+1mm) rectangle ++(2mm, 2mm); 
      \fill[storyblack!\c!headred] (\i mm, -\headheight+3mm) rectangle ++(2mm, 2mm);       
	  \fill[normalpage, opacity={(1-\srtmp)-0.5}] (\i mm, -\headheight+3mm) rectangle ++(2mm, 2mm);      	      
      }
	  \end{tikzpicture}
\end{document}